---
import Base from './Base.astro';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const { title = 'Project', description, ogImage } = Astro.props;
---

<Base title={title} description={description} ogImage={ogImage}>
  <slot />
</Base>
